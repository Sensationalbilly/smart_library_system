# AWS Configuration
AWS_REGION=us-east-1
AWS_PROFILE=default

# Environment
NODE_ENV=development
STAGE=dev

# Application
LOG_LEVEL=debug

# DynamoDB
DYNAMODB_TABLE_TRANSACTIONS=transactions-${STAGE}

# API Configuration
API_TIMEOUT=30000
